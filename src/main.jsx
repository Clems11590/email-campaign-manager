import React from 'react'
import ReactDOM from 'react-dom/client'
import EmailManagementTool from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EmailManagementTool />
  </React.StrictMode>,
)
